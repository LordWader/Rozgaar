extends ../layout.pug

block content
      div
        form(method='POST', action="")
          h2 Scan the QR code of ADHAAR CARD
          br
          div#qr-canvas(name= 'qr')
            video#qr-canvas#video(autoplay='true')
          br
        form(method='POST', action="/scan")
          input.form-control.button(type="submit", value="SCAN", id="go")


      script(href='static/js/qcode-decoder.js')
      script(href='static/js/script.js')
